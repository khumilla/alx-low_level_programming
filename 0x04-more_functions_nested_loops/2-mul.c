#include "main.h"

/**
 * mul - multiplying two integers together
 * @a: first integer
 * @b: second integer
 * Return: success the product of both integers
 */

int mul(int a, int b)
{
	return (a * b);
}
