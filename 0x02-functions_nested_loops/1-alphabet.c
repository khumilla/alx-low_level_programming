#include "main.h"

/**
 * print_alphabet- a to z in lowercase
 * followed by a new line
 * Return: void
 */
void print_alphabet(void)
{
	char a = 'a';
	char z = 'z';

	while
		(a <= z);
	{
		_putchar (a);
		a++;
	}
	_putchar ('\n');
	return (void);
}
